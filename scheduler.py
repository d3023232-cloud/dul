# 🤠 Дуэль Бот

Telegram-бот для дуэлей между реальными пользователями с экономикой, донат-магазином, реферальной системой, полной админ-панелью и **динамической экономикой** (меняется без перезапуска бота).

## ⚡ Быстрый старт

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` (скопируйте из `.env.example`):
```bash
cp .env.example .env
```

3. Отредактируйте `.env`:
```env
# Токен бота от @BotFather (ОБЯЗАТЕЛЬНО)
BOT_TOKEN=1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZ

# ID админов через запятую (ОБЯЗАТЕЛЬНО)
ADMIN_IDS=123456789

# Канал для обязательной подписки
CHANNEL_ID=@your_channel
CHANNEL_LINK=https://t.me/your_channel
```

4. Запустите бота:
```bash
python main.py
```

## 🎮 Функционал для игроков

### Основная механика
- **Дуэли** — вызов реальных пользователей, случайный исход
- **Анимация** — три точки при ожидании ответа, перестрелка ASCII-артом
- **Экономика** — настраивается админом в реальном времени!

### Восстановление монет
- При балансе **0** — +1 монета каждые **N минут** (настраивается)
- Обычный игрок: **X восстановлений/день** (настраивается)
- VIP игрок: **Y восстановлений/день** (настраивается)
- Сброс лимита: **00:00 МСК**

### Донат-магазин
| Товар | Цена | Эффект |
|-------|------|--------|
| VIP Premium | 299 DC | Лимит восстановлений увеличивается |
| Сброс лимита | 50 DC | Обнулить счётчик на сегодня |
| Монеты | 5 DC = 1 монета | Пакеты 1/5/10/15 или своё количество |

### Реферальная система
- Пригласивший: **2 монеты + 1 DC**
- Реферал: **5 монет**
- За 18 дуэлей реферала: **3 DC**

## 🛡️ Админ-панель

**Команда:** `/admin` (только для ID из `ADMIN_IDS`)

### Возможности админа:

| Функция | Описание |
|---------|----------|
| 👥 **Список игроков** | Просмотр всех пользователей с пагинацией |
| 🔍 **Поиск игрока** | Поиск по ID, юзернейму или имени |
| 📊 **Статистика бота** | Игроки, дуэли, монеты в обороте, топ победителей |
| ➕ **Выдать/забрать монеты** | Мгновенное изменение баланса |
| ➕ **Выдать/забрать DC** | Управление донат-коинами |
| 👑 **Дать/снять VIP** | Переключение статуса |
| 🚫 **Забанить/разбанить** | Блокировка доступа |
| 📜 **Транзакции игрока** | Полная история операций |
| ⚔️ **Активные дуэли** | Все дуэли со статусами |
| 📢 **Рассылка** | Массовая отправка всем игрокам |
| ⚙️ **Настройки экономики** | **Меняйте баланс без перезапуска!** |

### ⚙️ Динамическая экономика

В админ-панели раздел **«Настройки экономики»** позволяет менять параметры **без перезапуска бота**:

| Параметр | Дефолт | Описание |
|----------|--------|----------|
| `max_coins` | 99999 | Максимальный баланс монет |
| `recovery_interval_minutes` | 6 | Минут между восстановлениями при 0 |
| `win_multiplier` | 2 | Множитель выигрыша (ставка × N) |
| `daily_recovery_limit_default` | 5 | Лимит восстановлений (обычный) |
| `daily_recovery_limit_vip` | 15 | Лимит восстановлений (VIP) |
| `start_coins` | 10 | Стартовый баланс новых игроков |
| `duel_cost` | 1 | Стоимость одной дуэли |

**Все изменения вступают в силу немедленно!**

### Логирование
Все действия админа записываются в `admin_logs`.

### Блокировка
Забаненные пользователи автоматически блокируются middleware.

## 📁 Структура проекта

```
duel_bot/
├── .env.example         # Шаблон переменных окружения
├── main.py              # Точка входа, middleware бана
├── config.py            # Конфигурация из .env
├── database.py          # SQLite + динамическая экономика
├── states.py            # FSM состояния
├── keyboards.py         # Все кнопки
├── scheduler.py         # Сброс лимитов в 00:00 МСК
├── requirements.txt     # Зависимости
├── README.md            # Документация
├── utils/
│   └── helpers.py       # Вспомогательные функции
└── handlers/
    ├── __init__.py      # Регистрация роутеров
    ├── start.py         # Старт, приветствие, подписка
    ├── duel.py          # Логика дуэлей (динам. экономика)
    ├── profile.py       # Профиль и статистика
    ├── shop.py          # Донат-магазин
    ├── referral.py      # Реферальная система
    └── admin.py         # Админ-панель + редактор экономики
```

## ⚙️ Переменные окружения (BotHost)

На хостинге **BotHost** (или любом другом) задайте эти переменные:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BOT_TOKEN` | Токен от @BotFather | `123456:ABC...` |
| `ADMIN_IDS` | ID админов через запятую | `123456789,987654321` |
| `CHANNEL_ID` | Канал для подписки | `@my_channel` |
| `CHANNEL_LINK` | Ссылка на канал | `https://t.me/my_channel` |

**Всё остальное** (экономика, цены, лимиты) настраивается через `/admin` → «⚙️ Настройки экономики» прямо в Telegram!

## 📝 Лицензия

MIT License
